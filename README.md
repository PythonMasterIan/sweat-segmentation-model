## 🛠 安裝方式（使用 conda）

```bash
git clone https://github.com/你的帳號/sweat-segmentation-model.git
cd sweat-segmentation-model
pip install miniconda
conda env create -f environment.yml
conda activate pjmk2.env
python cut.py
